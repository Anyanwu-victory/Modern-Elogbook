<div className="space-y-6">
  <h2 className="text-xl font-bold">Assigned Students</h2>

  {assignedStudents.length > 0 ? (
    <ul className="space-y-4">
      {assignedStudents.map((student) => (
        <li key={student._id} className="p-4 border rounded-md">
          <h3 className="text-lg font-semibold">
            {student.firstName} {student.lastName}
          </h3>
          <p className="text-sm text-muted-foreground">Matric Number: {student.matricNumber}</p>
          <p className="text-sm text-muted-foreground">Email: {student.email}</p>
          {student.institutionSupervisor && (
            <p className="text-sm text-muted-foreground">
              Institution Supervisor: {student.institutionSupervisor.firstName} {student.institutionSupervisor.lastName}
            </p>
          )}
        </li>
      ))}
    </ul>
  ) : (
    <p className="text-muted-foreground">No students assigned to you.</p>
  )}
</div>